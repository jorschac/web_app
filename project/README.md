Set your configuration before you run the App: 
  "npm install" at the root directory before you do anything else
  
To run the React app, do the followings at root directory:
  "unset HOST" 
  "npm start"
